ftl.parser = /*
 * Generated by PEG.js 0.10.0.
 *
 * http://pegjs.org/
 */
(function() {
  "use strict";

  function peg$subclass(child, parent) {
    function ctor() { this.constructor = child; }
    ctor.prototype = parent.prototype;
    child.prototype = new ctor();
  }

  function peg$SyntaxError(message, expected, found, location) {
    this.message  = message;
    this.expected = expected;
    this.found    = found;
    this.location = location;
    this.name     = "SyntaxError";

    if (typeof Error.captureStackTrace === "function") {
      Error.captureStackTrace(this, peg$SyntaxError);
    }
  }

  peg$subclass(peg$SyntaxError, Error);

  peg$SyntaxError.buildMessage = function(expected, found) {
    var DESCRIBE_EXPECTATION_FNS = {
          literal: function(expectation) {
            return "\"" + literalEscape(expectation.text) + "\"";
          },

          "class": function(expectation) {
            var escapedParts = "",
                i;

            for (i = 0; i < expectation.parts.length; i++) {
              escapedParts += expectation.parts[i] instanceof Array
                ? classEscape(expectation.parts[i][0]) + "-" + classEscape(expectation.parts[i][1])
                : classEscape(expectation.parts[i]);
            }

            return "[" + (expectation.inverted ? "^" : "") + escapedParts + "]";
          },

          any: function(expectation) {
            return "any character";
          },

          end: function(expectation) {
            return "end of input";
          },

          other: function(expectation) {
            return expectation.description;
          }
        };

    function hex(ch) {
      return ch.charCodeAt(0).toString(16).toUpperCase();
    }

    function literalEscape(s) {
      return s
        .replace(/\\/g, '\\\\')
        .replace(/"/g,  '\\"')
        .replace(/\0/g, '\\0')
        .replace(/\t/g, '\\t')
        .replace(/\n/g, '\\n')
        .replace(/\r/g, '\\r')
        .replace(/[\x00-\x0F]/g,          function(ch) { return '\\x0' + hex(ch); })
        .replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) { return '\\x'  + hex(ch); });
    }

    function classEscape(s) {
      return s
        .replace(/\\/g, '\\\\')
        .replace(/\]/g, '\\]')
        .replace(/\^/g, '\\^')
        .replace(/-/g,  '\\-')
        .replace(/\0/g, '\\0')
        .replace(/\t/g, '\\t')
        .replace(/\n/g, '\\n')
        .replace(/\r/g, '\\r')
        .replace(/[\x00-\x0F]/g,          function(ch) { return '\\x0' + hex(ch); })
        .replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) { return '\\x'  + hex(ch); });
    }

    function describeExpectation(expectation) {
      return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
    }

    function describeExpected(expected) {
      var descriptions = new Array(expected.length),
          i, j;

      for (i = 0; i < expected.length; i++) {
        descriptions[i] = describeExpectation(expected[i]);
      }

      descriptions.sort();

      if (descriptions.length > 0) {
        for (i = 1, j = 1; i < descriptions.length; i++) {
          if (descriptions[i - 1] !== descriptions[i]) {
            descriptions[j] = descriptions[i];
            j++;
          }
        }
        descriptions.length = j;
      }

      switch (descriptions.length) {
        case 1:
          return descriptions[0];

        case 2:
          return descriptions[0] + " or " + descriptions[1];

        default:
          return descriptions.slice(0, -1).join(", ")
            + ", or "
            + descriptions[descriptions.length - 1];
      }
    }

    function describeFound(found) {
      return found ? "\"" + literalEscape(found) + "\"" : "end of input";
    }

    return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
  };

  function peg$parse(input, options) {
    options = options !== void 0 ? options : {};

    var peg$FAILED = {},

        peg$startRuleIndices = { Start: 0 },
        peg$startRuleIndex   = 0,

        peg$consts = [
          function(program) {

              //# Start

              return program;
            },
          function() { return text() },
          function(module_name) {

              //# ModuleDeclaration

              console.log("module name: '" + module_name + "'");
              module.name = module_name;
            },
          ".",
          peg$literalExpectation(".", false),
          function() {

              //# ModulePath

              return text();
            },
          function(first, rest) {

              //# Declarations

              return buildList(first, rest, 1);
            },
          function(importItems) {

              //# ImportDeclaration

              console.log(importItems);
              module.importStatement(null, importItems)
            },
          " ",
          peg$literalExpectation(" ", false),
          "as",
          peg$literalExpectation("as", false),
          function(name, as) {

              //# ImportSingleItem

              return {
                type: "single",
                name: name,
                asName: extractOptional(as, 3)
              };
            },
          ",",
          peg$literalExpectation(",", false),
          function(first, rest) {

              //# ImportMultiItems

              var ret = extractList(rest, 3);
              ret.unshift(first);
              return ret;
            },
          "[",
          peg$literalExpectation("[", false),
          "]",
          peg$literalExpectation("]", false),
          function(path, list) {

              //# ImportList

              return {
                type: "list",
                path: path,
                importList: list
              }
            },
          "=",
          peg$literalExpectation("=", false),
          function(modifier, id, expr) {

              //# VariableDeclaration
              var ret = modifier =='const' ? new ftl.ImmutableValFn(id.name, expr) : new ftl.VarFn(id.name, expr)
              module.addFn(id.name, ret); 
              return ret
            },
          function(id, params, body) {

              //# FunctionDeclaration

              console.log('function id: ', id.name || id)
              console.log('expr: ', body)

              var is_operator = id.type == 'OperatorDeclaration' || id.type == 'PostfixOperatorDeclaration';
              if (is_operator)
                console.log('parameter list from operator: ', id.operands)
              console.log('parameter list from function: ', optionalList(params))

              var param_list = is_operator ? id.operands : optionalList(params);
              console.log('parameter list: ', param_list)
              var name = id.name || id

              var ret = body.script ? new ftl.NativeFunctionFn(name, param_list, body.script) :
                  new ftl.FunctionFn(name, param_list, body);
              module.addFn(name, ret);

              return ret;
            },
          "(",
          peg$literalExpectation("(", false),
          ")",
          peg$literalExpectation(")", false),
          function(elms) { return elms == null ? new ftl.TupleFn() : new ftl.TupleFn(... elms) },
          function(expr, params) {

              // ExpressionCurry

              return new ftl.ExprFn(expr, extractList(params, 1))
            },
          function(first, rest) {

              //# ParameterList
              console.log("first", first);console.log("rest", rest);
              console.log("param list:", buildList(first, rest, 3));
              var list = buildList(first, rest, 3);
              for (var i = 0; i < list.length; i++) {
                if (list[i] instanceof ftl.RefFn) {
                  list[i].tupleSeq = '_' + i;
                }
              }
              return list;
             },
          ":",
          peg$literalExpectation(":", false),
          function(id, expr) {

              //# Parameter

                var iid = extractOptional(id, 0);
                if (iid != null)
                  iid = iid.name;
                if (iid == null)
                  return expr;
                return new ftl.NamedExprFn(iid, expr)
              },
          "->",
          peg$literalExpectation("->", false),
          function(ex) {

              //# PipeExpression

              return ex
            },
          function(first, rest) {

              //# Expression

              console.log("first is ", first)
              var t = extractOptional(rest, 1);
              if (rest)
                console.log("rest is ", t)
              if (t == null)
                return first;
              
              return new ftl.PipeFn(first, t);
            },
          function(expr) {

                //# Executable

                module.addExecutable(expr);
                return expr;
              },
          function(first, rest) {

              //# Operator

              return text();
            },
          function(id) {

              //# OperandValueDeclaration

              id.setAsValueType();
              return id;
            },
          function(id, params) {

              // #OperandReferenceDeclaration

              id.setAsRefType();
              id.params = params;
              return id
            },
          function(first, rest) {

              //# PreInfixOperatorDeclaration

              var ops = extractList(rest, 1);
              var name = ops.length == 1 ? ops[0] : ops.join(' ');
              console.log("operators in operator declaration:", ops)
              var operands = [first].concat(extractList(rest, 3))
              console.log("operands in operator declaration:", operands)
              return {
                type: 'OperatorDeclaration',
                name: name,
                operands: new ftl.TupleFn(... operands)
              }
            },
          function(operand, op) {

              //# PostfixOperatorDeclaration

              console.log('PostfixOperatorDeclaration begin')
              return {
                type: 'PostfixOperatorDeclaration',
                name: op,
                operands: operand
              }
            },
          function(op, expr) {

              //# UnaryOperatorExpression
              console.log('op', op)
              console.log('expr', expr)
              return new ftl.PipeFn(expr, module.getAvailableFn(op.name || op));
            },
          function(expr, op) {

              //# PostfixOperatorExpression

              console.log('PostfixOperatorExpression: op', op)
              console.log('PostfixOperatorExpression: expr', expr)
              var op_fn = module.getAvailableFn(op);
              if (!op_fn)
                op_fn = new ftl.RefFn(op);
              return new ftl.PipeFn(expr, op_fn);
            },
          function(operand, rest) {

              // N_aryOperatorExpression

              var ops = extractList(rest, 1);
              var params = [new ftl.OperandFn(operand)].concat(extractList(rest, 3).map(operand => new ftl.OperandFn(operand)));
              return new N_aryOperatorExpressionFn(ops, params)      
            },
          "_",
          peg$literalExpectation("_", false),
          "0",
          peg$literalExpectation("0", false),
          function() {

              //# TupleSelector

              return new ftl.TupleSelectorFn(text().substring(1));
            },
          function(id) {return text()},
          function(id, index) {

              //# ArrayElementSelector

              console.log('got ArrayElementSelector', index);
              return new ftl.ArrayElementSelectorFn(module, id, index);
            },
          function(elms) {

              //# ArrayLiteral

              var lst = extractOptional(elms, 1);
              return lst == null ? new ftl.ConstFn([]) : lst
            },
          function(first, rest) {

              //# LiteralList

              var elms = buildList(first, rest, 3);
              var ret = [];
              for (var i = 0; i < elms.length; i++)
                ret.push(elms[i].value);
              return new ftl.ConstFn(ret)
            },
          function(id, params) {

              //# CallExpression

              return new ftl.CallExprFn(id.name, extractList(params, 1));
            },
          "{",
          peg$literalExpectation("{", false),
          "}",
          peg$literalExpectation("}", false),
          function() {return text()},
          function() {return {type:'native', script: text()}},
          peg$anyExpectation(),
          function(name) {

              //# Identifier

              var ret = new ftl.RefFn(name);
              return ret;
            },
          peg$otherExpectation("identifier"),
          function(first, rest) {

              //# IdentifierName

              return first + rest.join("");
            },
          "$",
          peg$literalExpectation("$", false),
          function() {

              //# NamespaceIdentifier
              return text();
            },
          "false",
          peg$literalExpectation("false", false),
          "fn",
          peg$literalExpectation("fn", false),
          "null",
          peg$literalExpectation("null", false),
          "true",
          peg$literalExpectation("true", false),
          "module",
          peg$literalExpectation("module", false),
          "import",
          peg$literalExpectation("import", false),
          "var",
          peg$literalExpectation("var", false),
          "const",
          peg$literalExpectation("const", false),
          /^[A-Z]/,
          peg$classExpectation([["A", "Z"]], false, false),
          /^[a-z]/,
          peg$classExpectation([["a", "z"]], false, false),
          /^[0-9]/,
          peg$classExpectation([["0", "9"]], false, false),
          /^[!%&*+\-.\/:<=>?\^|\xD7\xF7\u220F\u2211\u2215\u2217\u2219\u221A\u221B\u221C\u2227\u2228\u2229\u222A\u223C\u2264\u2265\u2282\u2283]/,
          peg$classExpectation(["!", "%", "&", "*", "+", "-", ".", "/", ":", "<", "=", ">", "?", "^", "|", "\xD7", "\xF7", "\u220F", "\u2211", "\u2215", "\u2217", "\u2219", "\u221A", "\u221B", "\u221C", "\u2227", "\u2228", "\u2229", "\u222A", "\u223C", "\u2264", "\u2265", "\u2282", "\u2283"], false, false),
          function() { return new ftl.ConstFn(true) },
          function() { return new ftl.ConstFn(false) },
          function(literal) { return literal },
          function() {
                return new ftl.ConstFn(parseFloat(text()));
              },
          /^[\-]/,
          peg$classExpectation(["-"], false, false),
          /^[1-9]/,
          peg$classExpectation([["1", "9"]], false, false),
          "e",
          peg$literalExpectation("e", true),
          /^[+\-]/,
          peg$classExpectation(["+", "-"], false, false),
          "0x",
          peg$literalExpectation("0x", true),
          /^[0-9a-f]/i,
          peg$classExpectation([["0", "9"], ["a", "f"]], false, true),
          "\"",
          peg$literalExpectation("\"", false),
          function(chars) { var str = text(); return new ftl.ConstFn(str.substr(1, str.length - 2)) },
          "'",
          peg$literalExpectation("'", false),
          "\\",
          peg$literalExpectation("\\", false),
          "b",
          peg$literalExpectation("b", false),
          "f",
          peg$literalExpectation("f", false),
          "n",
          peg$literalExpectation("n", false),
          "r",
          peg$literalExpectation("r", false),
          "t",
          peg$literalExpectation("t", false),
          "v",
          peg$literalExpectation("v", false),
          "x",
          peg$literalExpectation("x", false),
          "u",
          peg$literalExpectation("u", false),
          /^[\n\r]/,
          peg$classExpectation(["\n", "\r"], false, false),
          "\n",
          peg$literalExpectation("\n", false),
          "\r\n",
          peg$literalExpectation("\r\n", false),
          "\r",
          peg$literalExpectation("\r", false),
          "\t",
          peg$literalExpectation("\t", false),
          "\x0B",
          peg$literalExpectation("\x0B", false),
          "\f",
          peg$literalExpectation("\f", false),
          "\xA0",
          peg$literalExpectation("\xA0", false),
          peg$otherExpectation("comment"),
          "//",
          peg$literalExpectation("//", false),
          "/*",
          peg$literalExpectation("/*", false),
          "*/",
          peg$literalExpectation("*/", false)
        ],

        peg$bytecode = [
          peg$decode("%;y/V#;!.\" &\"/H$;y/?$;#.\" &\"/1$;y/($8%: %!!)(%'#($'#(#'#(\"'#&'#"),
          peg$decode("%;R/Q#;w/H$%;\"/0#;M/'$8\":!\" )(\"'#&'#/($8#:\"#! )(#'#(\"'#&'#"),
          peg$decode("%$%;M/2#2#\"\"6#7$/#$+\")(\"'#&'#0<*%;M/2#2#\"\"6#7$/#$+\")(\"'#&'#&/& 8!:%! )"),
          peg$decode("%;$/_#$%;x/,#;$/#$+\")(\"'#&'#06*%;x/,#;$/#$+\")(\"'#&'#&/)$8\":&\"\"! )(\"'#&'#"),
          peg$decode(";%./ &;*.) &;+.# &;3"),
          peg$decode("%;S/:#;w/1$;'/($8#:'#! )(#'#(\"'#&'#"),
          peg$decode("%%;\"/\x82#;I.o &%;5/e#$%2(\"\"6(7)/,#;5/#$+\")(\"'#&'#0<*%2(\"\"6(7)/,#;5/#$+\")(\"'#&'#&/#$+\")(\"'#&'#/'$8\":!\" )(\"'#&'#/h#%;w/J#2*\"\"6*7+/;$;w/2$;I.# &;5/#$+$)($'#(#'#(\"'#&'#.\" &\"/)$8\":,\"\"! )(\"'#&'#"),
          peg$decode("%;)/\x8F#$%;w/D#2-\"\"6-7./5$;w/,$;)/#$+$)($'#(#'#(\"'#&'#0N*%;w/D#2-\"\"6-7./5$;w/,$;)/#$+$)($'#(#'#(\"'#&'#&/)$8\":/\"\"! )(\"'#&'#"),
          peg$decode("%%;\"/0#;M/'$8\":!\" )(\"'#&'#/p#;w/g$20\"\"6071/X$;w/O$;'.\" &\"/A$;w/8$22\"\"6273/)$8':4'\"&\")(''#(&'#(%'#($'#(#'#(\"'#&'#"),
          peg$decode(";(.# &;&"),
          peg$decode("%;U.# &;T/f#;w/]$;I/T$;w/K$25\"\"6576/<$;w/3$;4/*$8':7'#&$ )(''#(&'#(%'#($'#(#'#(\"'#&'#"),
          peg$decode("%;O/q#;w/h$;9.) &;I.# &;5/S$;w/J$;,.\" &\"/<$;w/3$;0/*$8':8'#$\" )(''#(&'#(%'#($'#(#'#(\"'#&'#"),
          peg$decode("%29\"\"697:/N#;w/E$;..\" &\"/7$2;\"\"6;7</($8$:=$!!)($'#(#'#(\"'#&'#"),
          peg$decode("%;,/e#$%;w/,#;,/#$+\")(\"'#&'#/9#06*%;w/,#;,/#$+\")(\"'#&'#&&&#/)$8\":>\"\"! )(\"'#&'#"),
          peg$decode("%;//\x8F#$%;w/D#2-\"\"6-7./5$;w/,$;//#$+$)($'#(#'#(\"'#&'#0N*%;w/D#2-\"\"6-7./5$;w/,$;//#$+$)($'#(#'#(\"'#&'#&/)$8\":?\"\"! )(\"'#&'#"),
          peg$decode("%%;I/;#;w/2$2@\"\"6@7A/#$+#)(#'#(\"'#&'#.\" &\"/;#;w/2$;2/)$8#:B#\"\" )(#'#(\"'#&'#"),
          peg$decode(";G.# &;1"),
          peg$decode("%2C\"\"6C7D/C#;w/:$;2/1$;w/($8$:E$!!)($'#(#'#(\"'#&'#"),
          peg$decode("%;<.# &;4/J#%;w/,#;1/#$+\")(\"'#&'#.\" &\"/)$8\":F\"\"! )(\"'#&'#"),
          peg$decode("%;2/' 8!:G!! )"),
          peg$decode(";A.M &;F.G &;E.A &;I.; &;B.5 &;C./ &;-.) &;,.# &;@"),
          peg$decode("%%<2C\"\"6C7D=.##&&!&'#/B#;Z/9$$;Z0#*;Z&/)$8#:H#\"! )(#'#(\"'#&'#"),
          peg$decode("%;I/' 8!:I!! )"),
          peg$decode("%;I/2#;,/)$8\":J\"\"! )(\"'#&'#"),
          peg$decode(";7.# &;6"),
          peg$decode(";:.# &;;"),
          peg$decode("%;8/\x89#$%;w/>#;5/5$;w/,$;8/#$+$)($'#(#'#(\"'#&'#/K#0H*%;w/>#;5/5$;w/,$;8/#$+$)($'#(#'#(\"'#&'#&&&#/)$8\":K\"\"! )(\"'#&'#"),
          peg$decode("%;8/;#;w/2$;5/)$8#:L#\"\" )(#'#(\"'#&'#"),
          peg$decode(";=.) &;?.# &;>"),
          peg$decode("%;5/;#;w/2$;4/)$8#:M#\"\" )(#'#(\"'#&'#"),
          peg$decode("%;4/;#;w/2$;5/)$8#:N#\"\" )(#'#(\"'#&'#"),
          peg$decode("%;4/\x89#$%;w/>#;5/5$;w/,$;4/#$+$)($'#(#'#(\"'#&'#/K#0H*%;w/>#;5/5$;w/,$;4/#$+$)($'#(#'#(\"'#&'#&&&#/)$8\":O\"\"! )(\"'#&'#"),
          peg$decode("%2P\"\"6P7Q/k#2R\"\"6R7S.R &%;`/H#$;Y0#*;Y&/8$%<;K=.##&&!&'#/#$+#)(#'#(\"'#&'#/'$8\":T\" )(\"'#&'#"),
          peg$decode("%;I/\x93#;w/\x8A$20\"\"6071/{$2R\"\"6R7S.H &%;`/8#$;Y0#*;Y&/($8\":U\"!%)(\"'#&'#.# &;I/A$;w/8$22\"\"6273/)$8&:V&\"%\")(&'#(%'#($'#(#'#(\"'#&'#"),
          peg$decode(";P./ &;\\.) &;].# &;f"),
          peg$decode("%20\"\"6071/a#%;w/,#;D/#$+\")(\"'#&'#.\" &\"/@$;w/7$22\"\"6273/($8$:W$!\")($'#(#'#(\"'#&'#"),
          peg$decode("%;4/\x8F#$%;w/D#2-\"\"6-7./5$;w/,$;4/#$+$)($'#(#'#(\"'#&'#0N*%;w/D#2-\"\"6-7./5$;w/,$;4/#$+$)($'#(#'#(\"'#&'#&/)$8\":X\"\"! )(\"'#&'#"),
          peg$decode("%;I/r#;w/i$20\"\"6071/Z$;w/Q$$;./&#0#*;.&&&#/;$;w/2$22\"\"6273/#$+')(''#(&'#(%'#($'#(#'#(\"'#&'#"),
          peg$decode("%;I/e#$%;w/,#;,/#$+\")(\"'#&'#/9#06*%;w/,#;,/#$+\")(\"'#&'#&&&#/)$8\":Y\"\"! )(\"'#&'#"),
          peg$decode("%2Z\"\"6Z7[/\u0152#;w/\u0149$%$%%<2Z\"\"6Z7[.) &2\\\"\"6\\7]=.##&&!&'#/,#;H/#$+\")(\"'#&'#0T*%%<2Z\"\"6Z7[.) &2\\\"\"6\\7]=.##&&!&'#/,#;H/#$+\")(\"'#&'#&/& 8!:^! )/\xCC$$;G0#*;G&/\xBC$;w/\xB3$%$%%<2Z\"\"6Z7[.) &2\\\"\"6\\7]=.##&&!&'#/,#;H/#$+\")(\"'#&'#0T*%%<2Z\"\"6Z7[.) &2\\\"\"6\\7]=.##&&!&'#/,#;H/#$+\")(\"'#&'#&/& 8!:^! )/6$2\\\"\"6\\7]/'$8':_' )(''#(&'#(%'#($'#(#'#(\"'#&'#"),
          peg$decode("1\"\"5!7`"),
          peg$decode("%%<;[=.##&&!&'#/1#;J/($8\":a\"! )(\"'#&'#"),
          peg$decode("<%;K/9#$;L0#*;L&/)$8\":c\"\"! )(\"'#&'#=.\" 7b"),
          peg$decode(";V.5 &2d\"\"6d7e.) &2P\"\"6P7Q"),
          peg$decode(";K.# &;Y"),
          peg$decode("%;X/O#$;X.) &2P\"\"6P7Q0/*;X.) &2P\"\"6P7Q&/'$8\":f\" )(\"'#&'#"),
          peg$decode("%2g\"\"6g7h/8#%<;L=.##&&!&'#/#$+\")(\"'#&'#"),
          peg$decode("%2i\"\"6i7j/8#%<;L=.##&&!&'#/#$+\")(\"'#&'#"),
          peg$decode("%2k\"\"6k7l/8#%<;L=.##&&!&'#/#$+\")(\"'#&'#"),
          peg$decode("%2m\"\"6m7n/8#%<;L=.##&&!&'#/#$+\")(\"'#&'#"),
          peg$decode("%2o\"\"6o7p/8#%<;L=.##&&!&'#/#$+\")(\"'#&'#"),
          peg$decode("%2q\"\"6q7r/8#%<;L=.##&&!&'#/#$+\")(\"'#&'#"),
          peg$decode("%2s\"\"6s7t/8#%<;L=.##&&!&'#/#$+\")(\"'#&'#"),
          peg$decode("%2u\"\"6u7v/8#%<;L=.##&&!&'#/#$+\")(\"'#&'#"),
          peg$decode(";W.# &;X"),
          peg$decode("4w\"\"5!7x"),
          peg$decode("4y\"\"5!7z"),
          peg$decode("4{\"\"5!7|"),
          peg$decode("4}\"\"5!7~"),
          peg$decode(";T.A &;U.; &;O.5 &;R./ &;S.) &;P.# &;\\"),
          peg$decode("%;Q/& 8!:\x7F! ).. &%;N/& 8!:\x80! )"),
          peg$decode("%;d/>#%<;K.# &;Y=.##&&!&'#/#$+\")(\"'#&'#.M &%;^/C#%<;K.# &;Y=.##&&!&'#/($8\":\x81\"!!)(\"'#&'#"),
          peg$decode("%;_/T#2#\"\"6#7$/E$$;Y0#*;Y&/5$;a.\" &\"/'$8$:\x82$ )($'#(#'#(\"'#&'#.\x91 &%4\x83\"\"5!7\x84.\" &\"/Z#2#\"\"6#7$/K$$;Y/&#0#*;Y&&&#/5$;a.\" &\"/'$8$:\x82$ )($'#(#'#(\"'#&'#.? &%;_/5#;a.\" &\"/'$8\":\x82\" )(\"'#&'#"),
          peg$decode("2R\"\"6R7S.Q &%4\x83\"\"5!7\x84.\" &\"/<#;`/3$$;Y0#*;Y&/#$+#)(#'#(\"'#&'#"),
          peg$decode("4\x85\"\"5!7\x86"),
          peg$decode("%;b/,#;c/#$+\")(\"'#&'#"),
          peg$decode("3\x87\"\"5!7\x88"),
          peg$decode("%4\x89\"\"5!7\x8A.\" &\"/9#$;Y/&#0#*;Y&&&#/#$+\")(\"'#&'#"),
          peg$decode("%3\x8B\"\"5\"7\x8C/@#%$;e/&#0#*;e&&&#/\"!&,)/#$+\")(\"'#&'#"),
          peg$decode("4\x8D\"\"5!7\x8E"),
          peg$decode("%2\x8F\"\"6\x8F7\x90/G#$;g0#*;g&/7$2\x8F\"\"6\x8F7\x90/($8#:\x91#!!)(#'#(\"'#&'#.W &%2\x92\"\"6\x927\x93/G#$;h0#*;h&/7$2\x92\"\"6\x927\x93/($8#:\x91#!!)(#'#(\"'#&'#"),
          peg$decode("%%<2\x8F\"\"6\x8F7\x90./ &2\x94\"\"6\x947\x95.# &;p=.##&&!&'#/,#;H/#$+\")(\"'#&'#.B &%2\x94\"\"6\x947\x95/,#;j/#$+\")(\"'#&'#.# &;i"),
          peg$decode("%%<2\x92\"\"6\x927\x93./ &2\x94\"\"6\x947\x95.# &;p=.##&&!&'#/,#;H/#$+\")(\"'#&'#.B &%2\x94\"\"6\x947\x95/,#;j/#$+\")(\"'#&'#.# &;i"),
          peg$decode("%2\x94\"\"6\x947\x95/,#;q/#$+\")(\"'#&'#"),
          peg$decode(";k.N &%2R\"\"6R7S/8#%<;Y=.##&&!&'#/#$+\")(\"'#&'#.# &;o"),
          peg$decode(";l.# &;m"),
          peg$decode("2\x92\"\"6\x927\x93.} &2\x8F\"\"6\x8F7\x90.q &2\x94\"\"6\x947\x95.e &2\x96\"\"6\x967\x97.Y &2\x98\"\"6\x987\x99.M &2\x9A\"\"6\x9A7\x9B.A &2\x9C\"\"6\x9C7\x9D.5 &2\x9E\"\"6\x9E7\x9F.) &2\xA0\"\"6\xA07\xA1"),
          peg$decode("%%<;n.# &;p=.##&&!&'#/,#;H/#$+\")(\"'#&'#"),
          peg$decode(";l.; &;Y.5 &2\xA2\"\"6\xA27\xA3.) &2\xA4\"\"6\xA47\xA5"),
          peg$decode("%2\xA2\"\"6\xA27\xA3/F#%%;e/,#;e/#$+\")(\"'#&'#/\"!&,)/#$+\")(\"'#&'#"),
          peg$decode("4\xA6\"\"5!7\xA7"),
          peg$decode("2\xA8\"\"6\xA87\xA9.5 &2\xAA\"\"6\xAA7\xAB.) &2\xAC\"\"6\xAC7\xAD"),
          peg$decode("2\xAE\"\"6\xAE7\xAF.M &2\xB0\"\"6\xB07\xB1.A &2\xB2\"\"6\xB27\xB3.5 &2(\"\"6(7).) &2\xB4\"\"6\xB47\xB5"),
          peg$decode("<;u.# &;t=.\" 7\xB6"),
          peg$decode("%2\xB7\"\"6\xB77\xB8/q#$%%<;p=.##&&!&'#/,#;H/#$+\")(\"'#&'#0B*%%<;p=.##&&!&'#/,#;H/#$+\")(\"'#&'#&/#$+\")(\"'#&'#"),
          peg$decode("%2\xB9\"\"6\xB97\xBA/\x8C#$%%<2\xBB\"\"6\xBB7\xBC=.##&&!&'#/,#;H/#$+\")(\"'#&'#0H*%%<2\xBB\"\"6\xBB7\xBC=.##&&!&'#/,#;H/#$+\")(\"'#&'#&/2$2\xBB\"\"6\xBB7\xBC/#$+#)(#'#(\"'#&'#"),
          peg$decode("%2\xB9\"\"6\xB97\xBA/\x98#$%%<2\xBB\"\"6\xBB7\xBC.# &;p=.##&&!&'#/,#;H/#$+\")(\"'#&'#0N*%%<2\xBB\"\"6\xBB7\xBC.# &;p=.##&&!&'#/,#;H/#$+\")(\"'#&'#&/2$2\xBB\"\"6\xBB7\xBC/#$+#)(#'#(\"'#&'#"),
          peg$decode("%$;r.# &;s0)*;r.# &;s&/\xA5#$%$;q/&#0#*;q&&&#/E#$;r.# &;s/,#0)*;r.# &;s&&&#/#$+\")(\"'#&'#0\\*%$;q/&#0#*;q&&&#/E#$;r.# &;s/,#0)*;r.# &;s&&&#/#$+\")(\"'#&'#&/#$+\")(\"'#&'#"),
          peg$decode("$%$;r.# &;s0)*;r.# &;s&/,#;q/#$+\")(\"'#&'#/L#0I*%$;r.# &;s0)*;r.# &;s&/,#;q/#$+\")(\"'#&'#&&&#"),
          peg$decode("$;r.) &;q.# &;s0/*;r.) &;q.# &;s&")
        ],

        peg$currPos          = 0,
        peg$savedPos         = 0,
        peg$posDetailsCache  = [{ line: 1, column: 1 }],
        peg$maxFailPos       = 0,
        peg$maxFailExpected  = [],
        peg$silentFails      = 0,

        peg$result;

    if ("startRule" in options) {
      if (!(options.startRule in peg$startRuleIndices)) {
        throw new Error("Can't start parsing from rule \"" + options.startRule + "\".");
      }

      peg$startRuleIndex = peg$startRuleIndices[options.startRule];
    }

    function text() {
      return input.substring(peg$savedPos, peg$currPos);
    }

    function location() {
      return peg$computeLocation(peg$savedPos, peg$currPos);
    }

    function expected(description, location) {
      location = location !== void 0 ? location : peg$computeLocation(peg$savedPos, peg$currPos)

      throw peg$buildStructuredError(
        [peg$otherExpectation(description)],
        input.substring(peg$savedPos, peg$currPos),
        location
      );
    }

    function error(message, location) {
      location = location !== void 0 ? location : peg$computeLocation(peg$savedPos, peg$currPos)

      throw peg$buildSimpleError(message, location);
    }

    function peg$literalExpectation(text, ignoreCase) {
      return { type: "literal", text: text, ignoreCase: ignoreCase };
    }

    function peg$classExpectation(parts, inverted, ignoreCase) {
      return { type: "class", parts: parts, inverted: inverted, ignoreCase: ignoreCase };
    }

    function peg$anyExpectation() {
      return { type: "any" };
    }

    function peg$endExpectation() {
      return { type: "end" };
    }

    function peg$otherExpectation(description) {
      return { type: "other", description: description };
    }

    function peg$computePosDetails(pos) {
      var details = peg$posDetailsCache[pos], p;

      if (details) {
        return details;
      } else {
        p = pos - 1;
        while (!peg$posDetailsCache[p]) {
          p--;
        }

        details = peg$posDetailsCache[p];
        details = {
          line:   details.line,
          column: details.column
        };

        while (p < pos) {
          if (input.charCodeAt(p) === 10) {
            details.line++;
            details.column = 1;
          } else {
            details.column++;
          }

          p++;
        }

        peg$posDetailsCache[pos] = details;
        return details;
      }
    }

    function peg$computeLocation(startPos, endPos) {
      var startPosDetails = peg$computePosDetails(startPos),
          endPosDetails   = peg$computePosDetails(endPos);

      return {
        start: {
          offset: startPos,
          line:   startPosDetails.line,
          column: startPosDetails.column
        },
        end: {
          offset: endPos,
          line:   endPosDetails.line,
          column: endPosDetails.column
        }
      };
    }

    function peg$fail(expected) {
      if (peg$currPos < peg$maxFailPos) { return; }

      if (peg$currPos > peg$maxFailPos) {
        peg$maxFailPos = peg$currPos;
        peg$maxFailExpected = [];
      }

      peg$maxFailExpected.push(expected);
    }

    function peg$buildSimpleError(message, location) {
      return new peg$SyntaxError(message, null, null, location);
    }

    function peg$buildStructuredError(expected, found, location) {
      return new peg$SyntaxError(
        peg$SyntaxError.buildMessage(expected, found),
        expected,
        found,
        location
      );
    }

    function peg$decode(s) {
      var bc = new Array(s.length), i;

      for (i = 0; i < s.length; i++) {
        bc[i] = s.charCodeAt(i) - 32;
      }

      return bc;
    }

    function peg$parseRule(index) {
      var bc    = peg$bytecode[index],
          ip    = 0,
          ips   = [],
          end   = bc.length,
          ends  = [],
          stack = [],
          params, i;

      while (true) {
        while (ip < end) {
          switch (bc[ip]) {
            case 0:
              stack.push(peg$consts[bc[ip + 1]]);
              ip += 2;
              break;

            case 1:
              stack.push(void 0);
              ip++;
              break;

            case 2:
              stack.push(null);
              ip++;
              break;

            case 3:
              stack.push(peg$FAILED);
              ip++;
              break;

            case 4:
              stack.push([]);
              ip++;
              break;

            case 5:
              stack.push(peg$currPos);
              ip++;
              break;

            case 6:
              stack.pop();
              ip++;
              break;

            case 7:
              peg$currPos = stack.pop();
              ip++;
              break;

            case 8:
              stack.length -= bc[ip + 1];
              ip += 2;
              break;

            case 9:
              stack.splice(-2, 1);
              ip++;
              break;

            case 10:
              stack[stack.length - 2].push(stack.pop());
              ip++;
              break;

            case 11:
              stack.push(stack.splice(stack.length - bc[ip + 1], bc[ip + 1]));
              ip += 2;
              break;

            case 12:
              stack.push(input.substring(stack.pop(), peg$currPos));
              ip++;
              break;

            case 13:
              ends.push(end);
              ips.push(ip + 3 + bc[ip + 1] + bc[ip + 2]);

              if (stack[stack.length - 1]) {
                end = ip + 3 + bc[ip + 1];
                ip += 3;
              } else {
                end = ip + 3 + bc[ip + 1] + bc[ip + 2];
                ip += 3 + bc[ip + 1];
              }

              break;

            case 14:
              ends.push(end);
              ips.push(ip + 3 + bc[ip + 1] + bc[ip + 2]);

              if (stack[stack.length - 1] === peg$FAILED) {
                end = ip + 3 + bc[ip + 1];
                ip += 3;
              } else {
                end = ip + 3 + bc[ip + 1] + bc[ip + 2];
                ip += 3 + bc[ip + 1];
              }

              break;

            case 15:
              ends.push(end);
              ips.push(ip + 3 + bc[ip + 1] + bc[ip + 2]);

              if (stack[stack.length - 1] !== peg$FAILED) {
                end = ip + 3 + bc[ip + 1];
                ip += 3;
              } else {
                end = ip + 3 + bc[ip + 1] + bc[ip + 2];
                ip += 3 + bc[ip + 1];
              }

              break;

            case 16:
              if (stack[stack.length - 1] !== peg$FAILED) {
                ends.push(end);
                ips.push(ip);

                end = ip + 2 + bc[ip + 1];
                ip += 2;
              } else {
                ip += 2 + bc[ip + 1];
              }

              break;

            case 17:
              ends.push(end);
              ips.push(ip + 3 + bc[ip + 1] + bc[ip + 2]);

              if (input.length > peg$currPos) {
                end = ip + 3 + bc[ip + 1];
                ip += 3;
              } else {
                end = ip + 3 + bc[ip + 1] + bc[ip + 2];
                ip += 3 + bc[ip + 1];
              }

              break;

            case 18:
              ends.push(end);
              ips.push(ip + 4 + bc[ip + 2] + bc[ip + 3]);

              if (input.substr(peg$currPos, peg$consts[bc[ip + 1]].length) === peg$consts[bc[ip + 1]]) {
                end = ip + 4 + bc[ip + 2];
                ip += 4;
              } else {
                end = ip + 4 + bc[ip + 2] + bc[ip + 3];
                ip += 4 + bc[ip + 2];
              }

              break;

            case 19:
              ends.push(end);
              ips.push(ip + 4 + bc[ip + 2] + bc[ip + 3]);

              if (input.substr(peg$currPos, peg$consts[bc[ip + 1]].length).toLowerCase() === peg$consts[bc[ip + 1]]) {
                end = ip + 4 + bc[ip + 2];
                ip += 4;
              } else {
                end = ip + 4 + bc[ip + 2] + bc[ip + 3];
                ip += 4 + bc[ip + 2];
              }

              break;

            case 20:
              ends.push(end);
              ips.push(ip + 4 + bc[ip + 2] + bc[ip + 3]);

              if (peg$consts[bc[ip + 1]].test(input.charAt(peg$currPos))) {
                end = ip + 4 + bc[ip + 2];
                ip += 4;
              } else {
                end = ip + 4 + bc[ip + 2] + bc[ip + 3];
                ip += 4 + bc[ip + 2];
              }

              break;

            case 21:
              stack.push(input.substr(peg$currPos, bc[ip + 1]));
              peg$currPos += bc[ip + 1];
              ip += 2;
              break;

            case 22:
              stack.push(peg$consts[bc[ip + 1]]);
              peg$currPos += peg$consts[bc[ip + 1]].length;
              ip += 2;
              break;

            case 23:
              stack.push(peg$FAILED);
              if (peg$silentFails === 0) {
                peg$fail(peg$consts[bc[ip + 1]]);
              }
              ip += 2;
              break;

            case 24:
              peg$savedPos = stack[stack.length - 1 - bc[ip + 1]];
              ip += 2;
              break;

            case 25:
              peg$savedPos = peg$currPos;
              ip++;
              break;

            case 26:
              params = bc.slice(ip + 4, ip + 4 + bc[ip + 3]);
              for (i = 0; i < bc[ip + 3]; i++) {
                params[i] = stack[stack.length - 1 - params[i]];
              }

              stack.splice(
                stack.length - bc[ip + 2],
                bc[ip + 2],
                peg$consts[bc[ip + 1]].apply(null, params)
              );

              ip += 4 + bc[ip + 3];
              break;

            case 27:
              stack.push(peg$parseRule(bc[ip + 1]));
              ip += 2;
              break;

            case 28:
              peg$silentFails++;
              ip++;
              break;

            case 29:
              peg$silentFails--;
              ip++;
              break;

            default:
              throw new Error("Invalid opcode: " + bc[ip] + ".");
          }
        }

        if (ends.length > 0) {
          end = ends.pop();
          ip = ips.pop();
        } else {
          break;
        }
      }

      return stack[0];
    }

    // this is the module created during parsing
    var module = new ftl.Module('')

    // this is for building function / operator parameters
    var dummy_param_tuple = new ftl.TupleFn();

    // The following functions are used for parsing

    function join(value) {
      if (Array.isArray(value))
        return value.join("")
      return value
    }

    function extractOptional(optional, index) {
      return optional ? optional[index] : null;
    }

    function extractList(list, index) {
      var result = new Array(list.length);

      for (var i = 0; i < list.length; i++) {
        result[i] = list[i][index];
      }

      return result;
    }

    function buildList(first, rest, index) {
      return [first].concat(extractList(rest, index));
    }

    function optionalList(value) {
      return value !== null ? value : [];
    }

    function buildFirstRest(first, rest) {
      return (Array.isArray(rest) && rest.length == 0) ? first : buildList(first, rest, 1)
    }

    /**
     * Type for build only.
     */
    class BuildElement extends ftl.Fn {
      constructor() {
        super();
      }

      build() {
      }
    }

    /**
     * This class captures n-ary operator expression. It is transient during parsing and building.
     */
    class N_aryOperatorExpressionFn extends BuildElement {
      constructor(ops, operands) {
        if (ops.length == 0)
          throw new ftl.FnConstructionError('No ops found!')

        super();
        this.ops = ops;
        this.operands = operands;
      }

      build(module, inputFn) {
        var current_index = 0;
        var stop_index = this.ops.length;

        // This is used to parse operators and operands recursively.
        // It is called from index = length of operators down to 1.
        function parse_operators(ops, operands, index, full) {

          // operand at index 1 is for operator at 
          var op = index == 1 ? ops[0] : ops.slice(0, index).join(' ')
          var f = module.getAvailableFn(op);

          // no corresponding function found for single op
          if (!f) {
            if (index == 1)
              throw new ftl.FtlBuildError("No function with name '" + op + "' found!");

            index--;
            var reduced = parse_operators(ops, operands, index, false);
            
            if (current_index == stop_index)
              return reduced;

            ops = ops.slice(index, ops.length)
            operands = [reduced].concat(operands.slice(index + 1, operands.length))
            return parse_operators(ops, operands, ops.length, true)
          }

          for (var i = 0; i < f.paramsInfo.fnodes.length; i++) {
            var fnode = f.paramsInfo.fnodes[i];
            if (fnode instanceof ftl.RefFn && fnode.isRefType()) {
              operands[i] = new ftl.ExprRefFn(operands[i], fnode.params, fnode.is_tail);
            }
          }

          current_index += index;
          var operands_tuple = new ftl.TupleFn(... operands.slice(0, f.params.fnodes.length)).build(module, dummy_param_tuple);
          
          return new ftl.PipeFn(operands_tuple, f);
        }

        return parse_operators(this.ops, this.operands, this.ops.length, true);
      }
    }

    // end of script for parser generation

    peg$result = peg$parseRule(peg$startRuleIndex);

    if (peg$result !== peg$FAILED && peg$currPos === input.length) {
      return module;
    } else {
      if (peg$result !== peg$FAILED && peg$currPos < input.length) {
        peg$fail(peg$endExpectation());
      }

      throw peg$buildStructuredError(
        peg$maxFailExpected,
        peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
        peg$maxFailPos < input.length
          ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1)
          : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
      );
    }
  }

  return {
    SyntaxError: peg$SyntaxError,
    parse:       peg$parse
  };
})();
